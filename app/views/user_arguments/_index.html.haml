- unless question.name == 'All'
  - question.user_conclusions(user).each do |node| 
    %h3= node.title
    = render :partial => 'user_arguments/show', :locals => {:node => node, :previous => previous, :original => node.id}

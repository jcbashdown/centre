.row-fluid
  .input-append.span12
    .row-fluid
      = f.text_field :title, :placeholder=>"Point", :class=>"typeahead span9", :autocomplete => "off", :value =>(session["nodes_query"] ? session["nodes_query"] : "")
      -if (is_conclusion ||= nil) && is_conclusion
        = f.hidden_field :is_conclusion, :value => true
      = f.submit 'Add/View', :class => 'btn span3'

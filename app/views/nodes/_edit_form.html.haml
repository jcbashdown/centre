-if current_user
  = form_for node, :as => :node, :url => node_path(node), :method => :put, :remote => true, :html => {:class => 'form-inline node-edit', :id => node.id} do |f|
    %h6.right
      Conclusion?&nbsp;
    = render :partial => "nodes/form_elements/is_conclusion", :locals => {:f => f, :context_node => (context_node ||= nil)}
